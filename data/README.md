# Data

Large source drawings, generated synthetic sets, and model weights are intentionally ignored by Git. Use only documented, permitted sources. Bootstrap fixtures are source code data, not a benchmark claim.
