# Data provenance

SLDForge controlled synthetic samples are the bootstrap data source. Ground truth is explicitly marked `synthetic_ground_truth`; it must never be represented as real-utility measurement. User uploads will retain the original file, normalized identifiers, and evidence references locally.
