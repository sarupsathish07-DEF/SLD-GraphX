# Local-first security

Uploads are treated as untrusted, stored under project-controlled paths, and never executed. The judging-critical path must not need cloud APIs, credentials, or remote inference. API keys and drawings are not committed.
