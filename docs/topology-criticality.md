# Topology criticality

Review priority is not raw low confidence. A candidate edge is ranked by confidence and topology impact, including downstream feeder reachability and bridge-like importance. Bootstrap exposes the data contract; ranking is implemented with live reconstructed graphs in the topology milestone.
