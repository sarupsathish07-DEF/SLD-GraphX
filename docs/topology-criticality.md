# Topology criticality

Review priority is not raw low confidence. M5 computes each uncertain edge with and without the edge, then stores affected feeder fraction, affected-node fraction, source assignment change, bridge/cut-edge importance, and component change. Risk is uncertainty times the configured impact score and drives CRITICAL/HIGH/MEDIUM/LOW sorting. These transparent weights are engineering design values, not learned scientific constants.
