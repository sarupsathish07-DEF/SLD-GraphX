# Model register

No OCR or symbol-detector weights are installed in Bootstrap 0. The architecture reserves adapter interfaces and will record source, license, checksum, local path, and classes before a model is accepted.
