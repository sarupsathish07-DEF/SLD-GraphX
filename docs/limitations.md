# Limitations

Bootstrap 0 is a deterministic graph-to-SVG/UI proof. It does not yet implement raster ingestion, CV symbol inference, OCR, conductor tracing, DXF parsing, manual correction persistence, or measured evaluation. It provides graph connectivity reasoning only, not power-flow, fault, or IEC compliance analysis.
